import { describe, it, expect } from "vitest";

describe("app scaffold", () => {
  it("renders without errors", () => {
    // Basic smoke test to verify the scaffold compiles
    expect(true).toBe(true);
  });
});
